import { TextInput } from "flowbite-react";

export default function Topbar() {
  return (
    <div className=" fixed top-0 z-10 flex h-16 w-full flex-col items-center justify-center bg-backgroundOne">
      <TextInput />
    </div>
  );
}
